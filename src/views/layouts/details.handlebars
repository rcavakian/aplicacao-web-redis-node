<h1>{{user.first_name}} {{user.last_name}} [{{user.id}}]</h1>
<ul class="list-group">
    <li class="list-group-item"><strong>Login: </strong>{{user.login}}</li>
    <li class="list-group-item"><strong>Nome: </strong>{{user.first_name}}</li>
    <li class="list-group-item"><strong>Sobrenome: </strong>{{user.last_name}}</li>
    <li class="list-group-item"><strong>E-mail: </strong>{{user.email}}</li>
</ul>
<div class="buttons-container">
    <a class="btn btn-primary btn-voltar" href="/">Voltar</a>
    <form method="POST" action="/user/delete/{{user.id}}?_method=DELETE">
        <input type="submit" class="btn btn-danger" value="Remover">
    </form>
</div>